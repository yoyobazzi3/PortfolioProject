<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio</title>
</head>
<body>
    <h1>Welcome to the portfolio of <%= username %></h1>

    <!-- Display the uploaded file if it exists -->
    <% if (profileFile) { %>
        <h2>Uploaded File:</h2>
        <!-- Check if the file is an image -->
        <% if (profileFile.endsWith('.jpeg') || profileFile.endsWith('.png')) { %>
            <img src="/uploads/<%= profileFile %>" alt="Profile Image" style="max-width: 200px;">
            <% } else if (profileFile.endsWith('.pdf')) { %>
            <embed src="/uploads/<%= profileFile %>" type="application/pdf" width="600" height="400" />
        <% } else { %>
            <a href="/uploads/<%= profileFile %>" download>Download File</a>
        <% } %>
    <% } else { %>
        <p>No file uploaded yet.</p>
    <% } %>

    <!-- Form to upload a new file -->
    <form action="/upload" method="post" enctype="multipart/form-data">
        <input type="hidden" name="username" value="<%= username %>">
        <input type="file" name="profileFile" required>
        <button type="submit">Upload File</button>
    </form>
</body>
</html>
